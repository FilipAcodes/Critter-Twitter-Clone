import React from "react";
import styled from "styled-components";

const Bookmarks = () => {
  return <StyledDivForTitle>Bookmarks</StyledDivForTitle>;
};

export default Bookmarks;

const StyledDivForTitle = styled.div``;
